# Tier base: Lambda standard per dev.
standard:
  runtime: nodejs24.x
  handler: index.handler
  architectures: ["x86_64"]
  memory_size: 512
  timeout: 30
  publish: true
  ignore_source_code_hash: true
  cloudwatch_logs_retention_in_days: 14

# Tier alternativo: Lambda standard per dev.
standard_external_code_bucket:
  runtime: nodejs24.x
  handler: index.handler
  architectures: ["x86_64"]
  memory_size: 512
  timeout: 30
  publish: true
  ignore_source_code_hash: true
  cloudwatch_logs_retention_in_days: 14
